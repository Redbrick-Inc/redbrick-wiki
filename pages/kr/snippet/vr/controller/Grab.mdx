---
title: "VR Grab 관련 함수 소개"
description: "VR Grab 관련 함수들을 알아봅시다."
hideTitle: true
---

import { Steps, Cards, Callout } from "nextra/components";

# Grab 관련 함수 소개

> Grab 관련 함수들을 알아봅시다.

---

## Grab 버튼 처음 누를 때, 뗄 때 이벤트 받기

아래와 같은 코드를 통해 양쪽 Grab 버튼의 처음 누를 때, 뗄 때의 타이밍을 알 수 있습니다.

```js showLineNumbers
// VR 기본 세팅
const avatar = REDBRICK.AvatarManager.createDefaultAvatar();
const camera = WORLD.getObject("MainCamera");
const followingCamera = avatar.setFollowingCamera(camera);
avatar.setDefaultController();
followingCamera.useVR({ VRObject: avatar });

// 왼쪽 Grab 버튼 처음 누를 때
function OnVRControllerLeftSqueezeStart(e) {
  console.log("HELLO SQUEEZE LEFT. ", e);
}

// 왼쪽 Grab 버튼 누르고 있다가 뗄 때
function OnVRControllerLeftSqueezeEnd(e) {
  console.log("BYE. SQUEEZE LEFT  ", e);
}

// 오른쪽 Grab 버튼 처음 누를 때
function OnVRControllerRightSqueezeStart(e) {
  console.log("HELLO SQUEEZE RIGHT. ", e);
}

// 오른쪽 Grab 버튼 누르고 있다가 뗄 때
function OnVRControllerRightSqueezeEnd(e) {
  console.log("BYE. SQUEEZE RIGHT  ", e);
}
```

아래 영상 결과에서 양쪽의 Grab 버튼을 누르고 뗄 때, 로그가 잘 찍히는 것을 확인할 수 있습니다. <br/>

<br />
<img
  src="/images/snippet/vr/controller/grab/grab_img1.gif"
  alt="grab_img1"
  loading="lazy"
  decoding="async"
  style={{ width: "100%", height: "auto" }}
/>
