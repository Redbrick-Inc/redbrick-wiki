# Scene

> 장면 객체에 추가되는 API들.

## 메소드

### getObject(title)

주어진 제목과 일치하는 장면 내의 객체를 찾아서 반환합니다.

- title : 찾고 있는 객체의 제목.

```js /getObject/
const object = WORLD.getObject("objectTitle");
const gui = GUI.getObject("guiTitle");
```
