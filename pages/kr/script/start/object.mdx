# Object Script

> 오브젝트 스크립트를 추가하고 사용하는 기본적인 방법에 대해 안내합니다.

싱글 플레이 게임을 제작한다면 여러분이 사용할 스크립트의 종류는 오브젝트와 클라이언트 이렇게 두가지로 나뉠 것입니다.  
오브젝트 스크립트란 오브젝트 자체에 연결된 스크립트를 말합니다.  
  
오브젝트의 속성 창에서 오브젝트 스크립트를 추가 / 편집 / 삭제할 수 있습니다. 또한 스크립트의 이름을 변경할 수 있습니다.

![object-script](../../../../images/scriptadd.png)

오브젝트 스크립트를 사용하지 않고 클라이언트 스크립트를 사용한다면, 특정 오브젝트를 불러올 때 `getObject()`등의 방법을 사용해 직접 지정해 주어야 합니다.  

```js showLineNumbers filename="클라이언트 스크립트"
const score_board = GUI.getObject("score_board"); //어떤 오브젝트를 가져올지 직접 지정해 주어야 함
score_board.setText("100");
```

그러나 오브젝트 스크립트를 사용한다면, 스크립트 내부에서 `this` 키워드를 이용해 해당 스크립트가 작성된 오브젝트에 바로 접근할 수 있습니다.

```js showLineNumbers copy {2} filename="GUI 오브젝트 score_board의 오브젝트 스크립트"
this.setText("100"); //this를 이용해 오브젝트에 직접 접근가능
```

아래 사진과 같이 좌측 상단 메뉴의 버튼을 클릭하면 모든 스크립트의 리스트를 확인할 수 있습니다.

![scriptlist](../../../../images/object&client-script-list.png)
