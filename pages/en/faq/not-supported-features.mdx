import { Callout } from "nextra/components";
import { Steps } from "nextra/components";
import { Tabs } from "nextra/components";

# Unsupported Features

Currently, the following features are not supported. Stay tuned, as we may add them in the near future. 😀

<Steps>

### How can I reduce the character size?

Currently, this feature is not available.

### Can I add a _'GIF'_ file and use it as a GUI?

Unfortunately, no. However, you can use `tween` or `move` functionalities to animate the GUI.

### Can I change the control key for the avatar to something other than 'WASD'?

Currently, this is not possible.

</Steps>
