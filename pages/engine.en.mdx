import { Callout } from "nextra/components";

# Engine

<Callout type="warning" emoji="⚠️">
  The engine is under constant updates. (e.g. the global `PLAYER` variable that
  existed before cannot be used now. However, it might be back in the future.)
</Callout>
